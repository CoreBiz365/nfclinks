# NFC Links Service Deployment Configuration
# Deploy to: get.biz365.ai
# Uses: docker-compose.yml

version: '3.8'

# This coolify.yml references the docker-compose.yml file
# Coolify will use the docker-compose.yml for deployment